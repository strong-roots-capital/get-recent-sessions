{
  "name": "@strong-roots-capital/get-recent-sessions",
  "version": "1.0.4",
  "description": "Get boundaries between recent timeframes",
  "main": "dist/src/get-recent-sessions.js",
  "types": "dist/src/get-recent-sessions.d.ts",
  "scripts": {
    "prepublishOnly": "npm run compile",
    "compile": "npm run clean && tsc",
    "clean": "rm -rf  dist",
    "ava": "ava -s -v",
    "watch:ava": "nodemon -e 'ts json' -x 'npm run ava || true'",
    "test": "nyc ava",
    "coverage": "nyc report --reporter=text-lcov > coverage.lcov && codecov",
    "watch:test": "nodemon -e 'ts json' -x 'npm test || true'",
    "typedoc": "typedoc",
    "push-with-tags": "git push origin master && git push origin --tags",
    "push-and-publish": "npm run push-with-tags && npm publish"
  },
  "files": [
    "dist/src/"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/strong-roots-capital/get-recent-sessions.git"
  },
  "keywords": [
    "timeseries",
    "timeframe",
    "open",
    "close",
    "session"
  ],
  "author": "Eric Crosson <eric.s.crosson@utexas.edu> (https://github.com/strong-roots-capital)",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/strong-roots-capital/get-recent-sessions/issues"
  },
  "homepage": "https://github.com/strong-roots-capital/get-recent-sessions",
  "dependencies": {
    "@strong-roots-capital/first-full-week-of-year": "^1.0.0",
    "@strong-roots-capital/is-tradingview-format": "^1.2.2",
    "date-fns": "^2.8.1",
    "od": "^1.0.0",
    "ow": "^0.12.0"
  },
  "devDependencies": {
    "@types/node": "^12.12.7",
    "@strong-roots-capital/list-tradingview-formats": "^1.0.2",
    "ava": "^2.0.0",
    "codecov": "^3.1.0",
    "moment": "^2.24.0",
    "nyc": "^13.1.0",
    "ts-node": "^8.5.2",
    "typedoc": "^0.13.0",
    "typedoc-plugin-markdown": "^1.1.20",
    "typescript": "^3.7.2"
  },
  "ava": {
    "compileEnhancements": false,
    "files": [
      "!dist/**/*.d.ts",
      "test/**/*.ts"
    ],
    "extensions": [
      "ts"
    ],
    "require": [
      "ts-node/register"
    ]
  },
  "nyc": {
    "include": [
      "src/*.ts"
    ],
    "exclude": [
      "typings"
    ],
    "extension": [
      ".ts"
    ],
    "require": [
      "ts-node/register"
    ]
  },
  "publishConfig": {
    "access": "public"
  }
}
